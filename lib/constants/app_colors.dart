import 'package:flutter/material.dart';

class AppColorsLightMode {
  static Color mainColor = Colors.white54.withOpacity(0.5);

  static const Color subColor = Color(0xFF48319D);
}

class AppColorsDarkMode {
  static Color mainColor = Colors.black45.withOpacity(0.5);

  static const Color subColor = Color(0xFF48319D);
}

class SwitchColors {
  static Color mainColor = Colors.white54.withOpacity(0.5);
  
  static const Color subColor = Color(0xFF48319D);

  static const Color borderColor = Colors.white30;
}
